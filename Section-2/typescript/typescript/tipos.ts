(function () {
  const NOMBRE: string = "Mario";
  const APELLIDO: string = "Ortega";
  const EDAD: number = 27;
  const CASADO: boolean = true;
  const DATE: Date = new Date();

  // const SALIDA  = NOMBRE + " " + APELLIDO + " " + "( Edad: " + EDAD + ")";

  const SALIDA = `${NOMBRE} ${APELLIDO} (${EDAD})`;
});
